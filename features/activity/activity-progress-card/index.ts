/**
 * @file features/activity/activity-progress-card/index.ts
 * Public exports for the Progress report card feature.
 *
 * Purpose: Single import surface for Progress card presentation.
 * Used in: `views/progress` (Step 6).
 * Used for: Rendering each `ProgressTask` in the Progress grid.
 */

export {
  ActivityProgressCard,
  type ActivityProgressCardProps,
} from "@/features/activity/activity-progress-card/ui/activity-progress-card";
