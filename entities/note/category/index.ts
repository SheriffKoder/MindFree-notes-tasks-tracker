/**
 * @file entities/note/category/index.ts
 * Public domain surface for note categories (types and pure helpers only).
 */

export type { NoteCategory } from "@/entities/note/category/model/types";
