/**
 * @file features/auth/session-expiry/index.ts
 * Public exports for cross-tab auth session sync (no user store).
 */

export { AuthSessionSync } from "@/features/auth/session-expiry/ui/auth-session-sync";
